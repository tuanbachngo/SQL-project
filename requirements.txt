# ===== Core OCR stack (stable on Windows + Python 3.11) =====
paddlepaddle==2.6.2
paddleocr==2.7.0.3

# Avoid NumPy 2.x ABI issues
numpy==1.26.4

# OpenCV pinned for compatibility
opencv-python==4.6.0.66

# PaddleOCR runtime deps (explicit)
pillow
shapely
pyclipper
scikit-image
protobuf==3.20.3
lmdb
pyyaml
tqdm
requests

# PDF -> images (no PyMuPDF)
pdf2image

# Data handling
pandas
openpyxl
